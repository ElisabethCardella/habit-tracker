import { Button } from "@chakra-ui/react";

const Header = () => {
  return (
    <div className="header">
      <Button colorScheme="blue">Les restaurants près de moi</Button>{" "}
    </div>
  );
};

export default Header;
